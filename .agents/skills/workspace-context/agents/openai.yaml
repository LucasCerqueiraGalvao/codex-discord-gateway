interface:
  display_name: "Workspace Context"
  short_description: "Build actionable project context fast"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  default_prompt: "Use $workspace-context to map the selected project into a concise working context with stack, entrypoints, run paths, logs, and likely edit surfaces."
